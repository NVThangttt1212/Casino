import { ChanbgDirective } from './chanbg.directive';

describe('ChanbgDirective', () => {
  it('should create an instance', () => {
    const directive = new ChanbgDirective();
    expect(directive).toBeTruthy();
  });
});
